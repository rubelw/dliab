apiVersion: v1
kind: Secret
metadata:
  name: superset-secrets
  labels:
    app: postgres-secrets
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
stringData:
  password: {{ .Values.superset_password | quote }}